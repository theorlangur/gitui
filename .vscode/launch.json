{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(OSX) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/gitui",
            //"terminal": "external",
            //"console": "externalTerminal",
            "args": [],
            "cwd": "${workspaceRoot}",
        }
    ]
}